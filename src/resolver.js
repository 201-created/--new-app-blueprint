import Resolver from 'dangerously-set-unified-resolver';

// Use this to skip fallback. Will not render {{legacy-component}}
// import Resolver from 'dangerously-set-unified-resolver/turbo';

export default Resolver.extend();
